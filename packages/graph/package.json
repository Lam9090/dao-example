{
  "name": "@dao/graph",
  "license": "UNLICENSED",
  "version": "1.0.2",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dev",
    "create-local": "graph create --node http://localhost:8020/ dev",
    "remove-local": "graph remove --node http://localhost:8020/ dev",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dev",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
