{
  "name": "dao-example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private":true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^19.2.1",
    "@commitlint/config-conventional": "^19.1.0",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3"
  },
  "scripts": {
    "hardhat:lint-staged": "yarn workspace @dao/hardhat lint-staged",
    "next:lint": "yarn workspace @dao/frontend lint",
    "deploy":"yarn workspace @dao/hardhat deploy"
  }
}
